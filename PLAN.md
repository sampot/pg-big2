# 大老二（`pg-big2`）— 遊戲規劃文檔

> **用途：** 本 repo 的遊戲權威規格——coding agent 改動前必讀：這個遊戲是什麼、規則、設計限制、優化方向。
> **整理方式：** 從本 repo 實作反向整理（2026-08-23）。**改玩法先改此檔再改碼**；本檔與程式碼衝突時，以「規則（§3）」描述的設計意圖為準回報差異。
> **上游契約：** [PG-GAME-AGENT-GUIDE.md](https://github.com/sampot/playgrounds/blob/main/docs/PG-GAME-AGENT-GUIDE.md)（唯一必讀；本檔不重複其全文）· 型錄條目 `playgrounds/catalog/entries/pg-big2.yaml`

## 1. 一句話

你＋三名 AI 的四人輪流出牌：單張／對／三條與五張牌型互壓、方塊 3 強制開局、先出完即勝的台式大老二；致敬「大老二／鋤大 D」玩法類型，非任一商業作品復刻。

## 2. 定案速覽

| 項 | 值 |
| --- | --- |
| catalog id / kind / series | `pg-big2` / `game` / `桌遊` |
| status | `listed` |
| 模式 | 單機 4 人（你 vs 小梅／阿心／黑哥三名 AI） |
| 牌組 | 52 張標準牌；每人 13 張 |
| 牌力 | 3 最小 → 2 最大；花色 ♦＜♣＜♥＜♠（`cardPower = rank×4 + suit`） |
| 五張階 | 順子 1＜同花 2＜葫蘆 3＜鐵支 4＜同花順 5（`FIVE_RANK`）；順子不含 2 |
| 對手 AI | 單一難度：由小捨牌＋領牌避 2，偶爾對大單張過牌 |
| 素材 | DOM/CSS 牌面＋WebAudio 合成音效（無圖檔資產） |
| 交付形 | 純 HTML＋CSS＋ESM JS；無 build；測試＝**零**（見 §7） |

## 3. 完整規則（現行實作）

### 3.1 發牌與開局

- 52 張洗勻輪發四家各 13 張，手牌依 `cardPower` 升冪排序。
- 持方塊 3（id=0）者先出，且**首輪第一副牌必須含 ♦3**（`firstTrickConstraint`）；違反回「首輪須含方塊 3」。

### 3.2 牌型與大小（`classify`／`beats`）

- 合法張數：1、2、3、5（4 張與 >5 一律不合法）。單張比絕對牌力；對／三條須同點數，比較時取該組內最大花色（`rank×4+max(suit)`）。
- 五張牌型判定：同花順＞鐵支＞葫蘆＞同花＞順子；跨型比較直接用「型階 ×10⁶＋關鍵值」，故任何同花順壓過一切五張。關鍵值：順／同花／同花順比最大單張牌力；葫蘆比三條點數、鐵支比四張點數（皆 ×4，不含花色）。
- 順子為連續 rank index 且**排除 2（index 12）**——最大順子為 10JQKA，無繞 A 也無 JQKA2。
- 跟牌：張數必須相同；五張以內同型比大小，五張可比型階。不同張數一律不能出（回「壓不住上家」或「不是合法牌型」）。領牌（自由出）可出任意合法牌型。

### 3.3 輪替與 Pass

- 出牌後輪到下一家（跳過已出完者，guard ≤4）。首家（自由出牌者）不可 Pass。
- 當 Pass 數 ≥ 未出完人數 −1，桌面清空、最後出牌者重新獲得自由出牌權。
- **任一人出完手牌，遊戲立即結束**（`status="over"`，僅記錄第一名；名次賽制未實作）。

### 3.4 AI 行為（`ai.js`）

- 開局首出強制過濾出含 ♦3 的組合。
- 所有合法著按（張數升冪、牌力升冪）排序——偏好用最少、最小的牌墊掉。
- 跟單張時：若最小能壓的牌 `power > 40`（約 A／2 級）、自己手牌 >5 張，且有 35% 機率直接 Pass，避免早期倒出大牌。
- 領牌時優先出「軟牌」（整副最高 `cardPower < 48`，即不含 2），沒有軟牌才出排序首位。
- 節奏：UI 延遲 `550 + Math.random()×450` ms；AI 算錯被 `play()` 拒絕時 fallback 成 Pass（領牌例外會顯示警告）。

### 3.5 邊界處理

- 非法操作一律拒絕並回中文原因：未開局／還沒輪到／已出完／不是合法牌型／壓不住上家／手牌沒有這些／首輪須含方塊 3／首家不能 Pass；UI 以 status 列＋deny 音呈現，不用原生對話框。
- 回合推進、自由出牌判定都有 guard 防死迴圈；AI 與玩家共用同一組 `play()`／`pass()` 校驗，無特權路徑。

## 4. 操作與畫面

| 輸入 | 動作 |
| --- | --- |
| 點手牌 | 選取／取消（可多選；手牌橫向滑動） |
| 出牌 | 打出所選牌（須成合法牌型且壓過上家） |
| Pass | 跟牌時跳過（自由出牌時停用） |
| 取消選取 | 清空選牌 |
| 開局／重來 | 發牌／回待機（非破壞性，不需確認） |
| 音效鈕 | 開/關（僅記憶體） |

- 四方位版型：下=你、左=小梅(1)、上=阿心(2)、右=黑哥(3)；對手顯示 ≤10 張牌背＋剩餘數＋最後動作（Pass 或牌型＋牌面）。
- 中央牌區顯示當前桌面牌與「誰·型別」提示；回合者座位亮起。
- Mobile-first；`role="listbox"` 手牌具 aria 標籤；禁 `alert`／`confirm`／`prompt`。

## 5. 持久化（KV 權威）

| key | 內容 | 讀寫時機 |
| --- | --- | --- |
| （無） | 本 repo 完全未用 `/api/kv` 或 localStorage；勝場、音效偏好皆僅存記憶體，重載即失 | — |

- `functions.js` 為宿主 stub（回 `{ok,name,path}` JSON），無 KV 讀寫。新增跨局統計必走 `/api/kv/{key}` 且 key 帶 `pg-big2-` 前綴（宿主 KV 無 per-SAM 命名空間）；localStorage 只可當快取鏡像。

## 6. 美術／音效／署名

- 無外部素材：牌面為 DOM/CSS 文字（rank＋花色 span、紅色 class），音效全 WebAudio 振盪器合成（`audio.js`，master 0.24；deal/select/play/pass/win/deny/turn 音色）。無需 ATTRIBUTION 檔。
- AudioContext 手勢 unlock（首次 pointerdown）；每次 tone 前都會再嘗試 resume。
- 若未來加入素材：拷進 `assets/`、新增 `ATTRIBUTION.md`（CC0 也須署名）、同步 `sam-manifest.json` files。

## 7. 測試

**現況零測試**——repo 內沒有任何 `*.test.js`，也無 vitest 設定檔。本遊戲規則分支密度高（牌型分類、跨型比較、首輪約束），是四款中最需要測試網的一個。

最小必測建議（新增 `game.test.js`，`npx --yes vitest@latest run` 即可跑）：`classify` 全牌型（含假組合拒判：三帶二以外五張、含 2 順子）、`beats` 同型/跨五張型/不同張數三向、`firstTrickConstraint` 首輪必含 ♦3、`pass` 自由出牌權返還、`deal→出完即勝` 全流程迴圈、AI 不出非法牌（`chooseAiMove` 回傳必過 `play()` 校驗）。改動規則前先補齊上述再動工（TDD）。

## 8. 硬約束（不可違反）

1. 僅 HTML＋CSS＋JS（ESM）；**無 build**、不入庫 `node_modules`、不安套件；工具一律 `npx <pkg>` 臨時執行。
2. 禁瀏覽器原生 `alert`／`confirm`／`prompt`；錯誤訊息一律頁內 status 列。
3. Mobile-first；主操作不可 hover-only。
4. 分數/進度的唯一權威是 `/api/kv/{key}`（現行尚未使用）；禁止裸 localStorage 當權威，新增 key 必帶 `pg-big2-` 前綴。
5. 不自行載入 `sdk.js`；本作未引用 `window.PG`（vanilla 即可，勿無故引入）。
6. 改動可執行邏輯前先寫失敗測試（TDD）——本 repo 測試從零建立，見 §7。
7. 檔案清單變動須同步 `sam-manifest.json`（下載契約）。

## 9. 優化建議（可玩性與樂趣）

依優先級；實作前先在此登記並補測試（§7 清單先行）。原則：強化決策深度與重玩誘因，不改變「四人輪流壓牌、先出完勝」的核心認同。

**高優先**

1. **名次計分＋多局累計**：目前單局定生死、無跨局目標。加台式計分（第一名依剩牌扣其他三家分、2 與♠A 加倍），連打 N 局累計總分，存 `/api/kv/pg-big2-scoreboard`——把「贏一手」變成「贏一場」，重玩誘因直接翻倍。做法：`finished[]` 已在，補完整出完順序即可算名次。
2. **出牌輔助**：`legalMoves()` 已存在但 UI 未用。選牌時即時比對：可壓過上家的組合亮起、「手上已無能壓之牌」給一行提示——新手最大的挫敗源（不知能不能出）一次解掉，且純前端零成本。
3. **補測試網**：§7 清單落地。不是樂趣功能，但是其餘建議的安全網；先測後改。

**中優先**

4. **AI 升級**：現行只看自己手牌。加兩個便宜啟發式：（a）記 2 與♠A 是否已現（`lastPlays` 可推）；（b）下家剩 1–2 張時避免領小單張／小對。做法都在 `ai.js` 排序權內，風險低。
5. **鐵支／同花順獎勵變體**：台灣常見「五張牌型出完直接贏」或加分制。以家規選項提供（預設關），讓五張牌型從「只能跟五張」變成戰略目標。
6. **AI 個性化**：沿用既有名字給不同參數（小梅保守、阿心標準、黑哥激進搶出），調 pass 機率與軟牌門檻即可，提升每局節奏差異。

**低優先**

7. 出牌動畫：牌從手中飛至中央＋桌面牌淡出；力度不改規則。
8. 音效細分：Pass 與非法 deny 目前音色接近，區分開；勝利旋律隨名次變長。
